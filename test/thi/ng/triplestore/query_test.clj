(ns thi.ng.triplestore.query-test
  (:require
   [thi.ng.triplestore
    [api :as api]
    [query :as q]]
   [thi.ng.triplestore.impl.memory :as mem])
  (:use clojure.test))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
